import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Reslink Affiliate Programme · 45% Recurring for Two Years',
  description: 'Earn 45% recurring commission for two years on every customer you refer to Reslink. Free to join, monthly payouts, and every asset written for you.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
