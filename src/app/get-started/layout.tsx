import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Create Your Free Reslink Account",
  description: "Sign up free as a job seeker, student, or veteran \u2014 or set up your company, agency, or university. No credit card required.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
