import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Free Video Resume Script Templates \u00b7 Reslink",
  description: "Ready-to-use video resume scripts and templates for every industry and experience level.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
